node_modules/
dist/
*.log
.DS_Store

# Build artifacts: the bundled plugin.js IS committed (so the .sdPlugin is
# self-contained for drag-and-drop / Homebrew installs). Only the sourcemap
# and runtime logs are ignored.
com.movingavg.switchboard.sdPlugin/bin/plugin.js.map
com.movingavg.switchboard.sdPlugin/logs/

# Packaged installer + cask zip (produced by `npm run pack` / `npm run pack:zip`)
*.streamDeckPlugin

coverage/

# Fastlane: never commit signing keys or run artifacts.
*.p8
fastlane/auth_key.json
fastlane/report.xml
fastlane/Preview.html
fastlane/test_output/
Gemfile.lock
